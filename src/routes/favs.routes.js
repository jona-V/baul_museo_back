/*import { Router } from "express";
import {auth} from "../middlewares/auth.middleware.js";

import { 
    addFavs, 
    postFavs, 
    deleteFavs, 
   } from "../controllers/auth.favs.js";

const router = Router();

router.get("/favs", auth, addFavs);

router.post("/favs/:id", auth, postFavs);

router.delete("/favs/:id", auth, deleteFavs);

export default router;*/